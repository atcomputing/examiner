<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <ImageView
        style="@style/BackgroundImage"
        android:contentDescription="@string/student_hat" />

    <LinearLayout
        android:id="@+id/exam_buttonLayout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal" >

        <Button
            android:id="@+id/button_start_exam"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/start_a_new_exam"
            android:textSize="@dimen/textSizeDefault"
         	android:visibility="gone" />

        <Button
            android:id="@+id/button_prev"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="left"
            android:layout_weight="1"
            android:text="@string/button_prev"
            android:textSize="@dimen/textSizeDefault"
            android:visibility="gone" />

        <Button
            android:id="@+id/button_next"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="right"
            android:layout_weight="1"
            android:text="@string/button_next"
            android:textSize="@dimen/textSizeDefault"
            android:visibility="gone" />
    </LinearLayout>

    <FrameLayout
        android:id="@+id/exam_fragment_holder"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_above="@id/exam_buttonLayout"
        android:layout_alignParentTop="true"
        android:background="#80000000" />

</RelativeLayout>